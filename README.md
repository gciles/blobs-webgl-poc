# Blobs - WebGL

I love `blobs`.<br>
So this time, my love for `blobs` will be the excuse to see what we can do with a `sphere`, transforming it using `shaders`.

# Installation

```bash
npm i
npm start
npm build
---
yarn install
yarn start
yarn build
```